# WCL Worker

The 2.2.1 Worker is preserved here so we do not lose working OAuth/metadata/mechanics code during the RaidRU 3 rewrite.
It is not part of the alpha.1 web build. Before reconnecting it, its public contract will be documented and covered by integration fixtures.
